import { View, Text } from "react-native";

/*
 * Pantalla principal temporal del mapa.
 *
 * De momento se deja simple para cerrar la navegación
 * y quitar errores de rutas mientras montamos la base.
 */
export default function Mapa() {
    return (
        <View>
            <Text>Pantalla mapa</Text>
        </View>
    );
}